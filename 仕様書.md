# Ingredient2Recipe 仕様書

## 1. システム概要

Ingredient2Recipe は、楽天レシピAPIからカテゴリ一覧とカテゴリ別レシピランキングを取得し、ユーザーが選択したカテゴリに応じたレシピランキングを表示する Flask アプリケーションである。

現在の主要機能は以下の通り。

- 楽天レシピカテゴリ一覧APIからカテゴリ情報を取得する
- カテゴリを「大分類」「中分類」「小分類」の3段階で選択する
- 選択したカテゴリIDをもとに楽天レシピカテゴリ別ランキングAPIからレシピを取得する
- レシピランキング、画像、材料、調理時間、費用、レシピURLを表示する

## 2. 利用者向け機能

### 2.1 カテゴリ選択

トップページ `/` で、ユーザーは以下の順にカテゴリを選択できる。

1. 大分類
2. 中分類
3. 小分類

大分類は必須であり、中分類・小分類は任意である。最低限、大分類のみを選択すればレシピランキングを表示できる。

中分類の選択肢は、選択された大分類に属するものだけが表示される。小分類の選択肢は、選択された中分類に属するものだけが表示される。

スマートフォンで不要な選択肢がグレーアウト表示される問題を避けるため、中分類・小分類の選択肢は JavaScript で `<select>` の中身を都度再構築する。

### 2.2 レシピランキング表示

`/recipes` では、選択されたカテゴリIDに対応するレシピランキングを表示する。

カテゴリIDは以下の優先順位で決定する。

1. 小分類 `category3_id`
2. 中分類 `category2_id`
3. 大分類 `category1_id`

表示項目は以下の通り。

| 項目 | 内容 |
| --- | --- |
| レシピ名 | APIレスポンスの `recipeTitle` |
| レシピ画像 | APIレスポンスの `foodImageUrl` |
| 順位 | APIレスポンスの `rank` |
| 調理時間 | APIレスポンスの `recipeIndication` |
| 費用 | APIレスポンスの `recipeCost` |
| 材料 | APIレスポンスの `recipeMaterial` |
| レシピURL | APIレスポンスの `recipeUrl` |

## 3. 画面仕様

### 3.1 カテゴリ選択画面

| 項目 | 内容 |
| --- | --- |
| URL | `/` |
| テンプレート | `flaskr/templates/home.html` |
| JavaScript | `flaskr/templates/home.js` |
| CSS | `flaskr/templates/style.css` |
| 目的 | ユーザーがカテゴリを選択し、レシピランキング画面へ遷移する |

入力項目は以下の通り。

| name | 表示名 | 必須 | 説明 |
| --- | --- | --- | --- |
| `category1_id` | 大分類 | 必須 | ハイフン0個のカテゴリID |
| `category2_id` | 中分類 | 任意 | ハイフン1個のカテゴリID |
| `category3_id` | 小分類 | 任意 | ハイフン2個のカテゴリID |

送信先は `/recipes`、HTTPメソッドは GET である。

### 3.2 レシピランキング画面

| 項目 | 内容 |
| --- | --- |
| URL | `/recipes` |
| テンプレート | `flaskr/templates/recipes.html` |
| CSS | `flaskr/templates/style.css` |
| 目的 | 選択されたカテゴリのレシピランキングを表示する |

`selected_category_id` が空の場合は、カテゴリ未選択のメッセージを表示する。レシピが取得できない場合は、レシピランキングが見つからない旨を表示する。

## 4. カテゴリID仕様

楽天レシピAPIのカテゴリ情報をもとに、アプリ内では以下の形式で `category_id` を扱う。

| 分類 | 形式 | ハイフン数 | 例 |
| --- | --- | --- | --- |
| 大分類 | `大` | 0 | `10` |
| 中分類 | `大-中` | 1 | `10-20` |
| 小分類 | `大-中-小` | 2 | `10-20-30` |

`get_category_list()` はカテゴリ一覧APIのレスポンスを以下の列を持つ DataFrame に変換する。

| 列名 | 内容 |
| --- | --- |
| `category1` | 大分類ID |
| `category2` | 中分類ID。大分類行では空文字 |
| `category3` | 小分類ID。大分類・中分類行では空文字 |
| `category_id` | APIに渡すカテゴリID |
| `category_name` | カテゴリ名 |

## 5. ルーティング仕様

### 5.1 `GET /`

カテゴリ選択画面を表示する。

処理内容:

1. `getFromApi.get_category_list()` でカテゴリ一覧を取得する
2. 大分類・中分類・小分類の表示用データを作成する
3. `home.html` に以下の値を渡す

| テンプレート変数 | 内容 |
| --- | --- |
| `id_name_pairs_1` | 大分類のカテゴリIDとカテゴリ名 |
| `id_name_pairs_2` | 大分類ID、中分類カテゴリID、中分類名 |
| `id_name_pairs_3` | 中分類カテゴリID、小分類カテゴリID、小分類名 |

### 5.2 `GET /recipes`

レシピランキング画面を表示する。

クエリパラメータ:

| パラメータ | 内容 |
| --- | --- |
| `category1_id` | 大分類カテゴリID |
| `category2_id` | 中分類カテゴリID |
| `category3_id` | 小分類カテゴリID |

処理内容:

1. `category3_id`、`category2_id`、`category1_id` の順で有効なカテゴリIDを選ぶ
2. カテゴリIDが存在する場合、`getFromApi.get_recipe_ranking()` でランキングを取得する
3. DataFrameを辞書配列に変換し、`recipes.html` に渡す

### 5.3 `GET /home.js`

`flaskr/templates/home.js` を配信する。

### 5.4 `GET /style.css`

`flaskr/templates/style.css` を配信する。

## 6. 外部API連携

### 6.1 カテゴリ一覧API

`getFromApi.get_category_list()` が利用する。

環境変数 `CATEGORY_URL` に設定されたURLへリクエストを送信する。

### 6.2 カテゴリ別ランキングAPI

`getFromApi.get_recipe_ranking(category_id)` が利用する。

環境変数 `RECIPE_RANKING_URL` に設定されたURLへリクエストを送信する。リクエストパラメータ `categoryId` に選択済みカテゴリIDを指定する。

### 6.3 共通リクエスト情報

| 環境変数 | 用途 |
| --- | --- |
| `APPLICATION_ID` | 楽天APIのアプリケーションID |
| `ACCESS_KEY` | 楽天APIのアクセスキー |
| `CATEGORY_URL` | カテゴリ一覧API URL |
| `RECIPE_RANKING_URL` | カテゴリ別ランキングAPI URL |
| `BASE_URL` | リクエストヘッダー `Origin` / `Referer` の生成元 |

## 7. モジュール構成

```text
Ingredient2Recipe/
├── main.py
├── flaskr/
│   ├── __init__.py
│   ├── controllers/
│   │   └── controller.py
│   ├── services/
│   │   └── getFromApi.py
│   └── templates/
│       ├── home.html
│       ├── home.js
│       ├── recipes.html
│       └── style.css
├── README.md
├── 仕様書.md
└── pyproject.toml
```

### 7.1 `main.py`

Flaskアプリケーションを起動するエントリーポイント。

### 7.2 `flaskr/__init__.py`

Flaskアプリケーションインスタンスを作成し、Controllerを読み込む。

### 7.3 `flaskr/controllers/controller.py`

画面表示、リクエストパラメータの受け取り、テンプレートへのデータ受け渡しを担当する。

### 7.4 `flaskr/services/getFromApi.py`

楽天レシピAPIとの通信、APIレスポンスのDataFrame変換を担当する。

## 8. 未実装・改善候補

以下は現時点で未実装または改善余地のある項目である。

- `convert_name_to_id()` は未実装
- `filter_by_ingredient()` は未実装
- API通信失敗時の例外処理
- APIレスポンス形式が想定外だった場合のハンドリング
- カテゴリ一覧のキャッシュ
- `home.js` / `style.css` を Flask 標準の `static` ディレクトリから配信する構成への移行
- READMEに本番環境の環境変数設定手順を追加する