## バックエンド

### 処理フロー

1. ライブラリのインポート
2. API認証情報の取得
3. 楽天レシピカテゴリ一覧APIへリクエスト送信
4. カテゴリ一覧をDataFrameへ変換
5. フロントエンドへカテゴリ一覧を返却
6. ユーザー入力（カテゴリ・食材名）を受信
7. カテゴリ名からカテゴリIDへ変換
8. 楽天レシピカテゴリ別ランキングAPIへリクエスト送信
9. レシピ情報をDataFrameへ変換
10. 食材名による絞り込み処理を実施
11. レシピ名・レシピURLを抽出
12. フロントエンドへ返却

### API

#### GET /categories

##### 概要

楽天レシピカテゴリ一覧APIからカテゴリ情報を取得する。

##### レスポンス

| 項目           | 型      | 説明     |
| ------------ | ------ | ------ |
| categoryId   | int    | カテゴリID |
| categoryName | string | カテゴリ名  |

---

#### GET /recipes

##### 概要

カテゴリと食材名をもとにレシピを検索する。

##### リクエスト

| 項目             | 型      | 説明    |
| -------------- | ------ | ----- |
| categoryName   | string | カテゴリ名 |
| ingredientName | string | 食材名   |

##### レスポンス

| 項目          | 型      | 説明     |
| ----------- | ------ | ------ |
| recipeTitle | string | レシピ名   |
| recipeUrl   | string | レシピURL |

### クラス・関数

#### 食材一覧取得クラス

##### 責務

カテゴリ一覧の取得および画面表示用データの生成を担当する。

##### 属性

なし

##### 操作

* 食材一覧の取得（get）

---

#### API連携クラス（GetInformationFromApi）

##### 責務

楽天レシピAPIとの通信処理を担当する。

##### 属性

* applicationId
* categoryApiUrl
* recipeApiUrl

##### 操作

* カテゴリー一覧の取得（getCategory() -> DataFrame）
* レシピ一覧の取得（getRecipe(int categoryId) -> DataFrame）

---

#### フロントエンド入力値加工クラス

##### 責務

ユーザー入力値の変換およびレシピ絞り込み処理を担当する。

##### 属性

なし

##### 操作

* カテゴリー名からカテゴリーIDへの変換

  * categoryName2CategoryId(str categoryName) -> int categoryId

* 食材によるレシピ絞り込み

  * recipeFromIngredient(str ingredientName) -> str recipe, str recipeUrl
